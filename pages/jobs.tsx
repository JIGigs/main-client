import type { NextPage } from 'next';
import { MainLayout } from '../layouts';

const Jobs: NextPage = () => {
  return (
    <MainLayout>
      <h1 className='text-3xl'>Jobs page</h1>
    </MainLayout>
  );
};

export default Jobs;
